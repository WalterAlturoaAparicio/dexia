name: dex_ia
description: "Clasificador de aves colombianas con EfficientNet-B0 ONNX."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  onnxruntime: ^1.4.1
  image_picker: ^1.1.2
  image: ^4.2.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/classes.json
    - assets/efficientnet_b0_best.onnx
